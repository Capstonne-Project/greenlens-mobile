/**
 * Field worker API — Cleaner + CompanyStaff (master plan §4).
 * Implementation giữ nguyên; tên module chuẩn hóa theo master plan.
 */
export { cleanupAssignmentService as fieldWorkerService } from '@/services/cleanupAssignment.service';
